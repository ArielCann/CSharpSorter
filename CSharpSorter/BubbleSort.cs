using System;
public class BubbleSort
{
	public void buildChart(int[] nums_to_sort)
	{
		for (int i = 0; i < nums_to_sort.Length; i++)
		{
            Pen pen = new Pen(Color.Blue);
		}

	}
	public void clear()
	{

	}
	public BubbleSort(int[] nums_to_sort)
	{
		buildChart(nums_to_sort);
	}
}
